{
  "name": "chelp-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker Edge API & Pro Inference Proxy for CommandHelp",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240903.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.75.0"
  }
}
